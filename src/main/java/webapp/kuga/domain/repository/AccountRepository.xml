<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="webapp.domain.repository.AccountMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO account (title, details, finished) VALUES (#{title}, #{details}, #{finished})
    </insert>
    <select id="find" resultType="webapp.domain.model.Account">
        SELECT id, title, details, finished FROM account WHERE id = #{id}
    </select>
</mapper>